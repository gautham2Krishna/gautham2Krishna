#include <stdio.h>
int main() {
    int n;
    printf("enter a any number:");
    scanf("%d", &n);
    printf("factors of %d are : /n",n);
    
    for (int i = 2; i <= n; ++i)
    {
        if (n % i == 0) 
            printf("%d ", i);
        }
    }
    
